interface:
  display_name: "Pull Request"
  short_description: "Open a PR only when asked"
  default_prompt: "Use $pull-request only if I asked for a pull request."
policy:
  allow_implicit_invocation: false
