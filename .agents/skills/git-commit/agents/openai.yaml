interface:
  display_name: "Git Commit"
  short_description: "Commit only when asked"
  default_prompt: "Use $git-commit only if I asked to commit. Follow its safety protocol."
policy:
  allow_implicit_invocation: false
