interface:
  display_name: "App Builder"
  short_description: "Implementation workflow for selected app changes"
  default_prompt: "Use $orchestrator first, then $app-builder only if named. Implement the smallest correct change. Do not load unused skill files."
policy:
  allow_implicit_invocation: false
