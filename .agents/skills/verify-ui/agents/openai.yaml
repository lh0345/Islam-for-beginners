interface:
  display_name: "Verify UI"
  short_description: "Verify user-visible UI when needed"
  default_prompt: "Use $verify-ui only after a user-visible web UI change or if I asked to verify."
policy:
  allow_implicit_invocation: false
